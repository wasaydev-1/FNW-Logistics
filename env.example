# Namecheap Domain Email Configuration
# Replace with your actual Namecheap email credentials

# SMTP Server Settings
SMTP_HOST=mail.privateemail.com
SMTP_PORT=587
SMTP_SECURE=false

# Email Credentials
SMTP_USER=wahab@fnw-logistics.com
SMTP_PASS=your-email-password

# Instructions for Namecheap Email Setup:
# 1. Log into your Namecheap account
# 2. Go to "Domain List" and click "Manage" next to your domain
# 3. Go to "Advanced DNS" tab
# 4. Make sure you have MX records pointing to Namecheap mail servers
# 5. Create email account: wahab@fnw-logistics.com
# 6. Use the email password you set for SMTP_PASS
# 7. Common Namecheap SMTP settings:
#    - Host: mail.privateemail.com (Namecheap's mail server)
#    - Port: 587 (for TLS) or 465 (for SSL)
#    - Security: TLS (port 587) or SSL (port 465)